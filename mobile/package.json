{
  "name": "open-cursor-mobile",
  "version": "1.1.0",
  "type": "module",
  "description": "Authenticated mobile companion for Open-Cursor",
  "main": "server.js",
  "scripts": {
    "check": "node --check server.js && node --check server.test.js",
    "test": "node --test server.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
